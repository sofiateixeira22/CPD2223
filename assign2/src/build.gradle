subprojects {
    apply plugin: 'java'

    group 'com.cpd'
    version '1.0'

    tasks.withType(JavaCompile) {
        options.encoding = 'UTF-8'
    }

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(17)
        }
    }
}
